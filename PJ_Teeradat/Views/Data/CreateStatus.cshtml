@model PJ_Teeradat.Models.TbStatus

@{
    ViewData["Title"] = "CreateStatus";
    Layout = "~/Views/Shared/_Layout_User.cshtml";
}
@{ if (ViewBag.UserType == "2")
    {
        <h2>CreateStatus</h2>

        <h4>เพิ่มข้อมูล</h4>
        <hr />
        <div class="row">
            <div class="col-md-4">
                <form asp-action="CreateStatus">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="StatusName" class="control-label">ชื่อสถานะ</label>
                        <input asp-for="StatusName" class="form-control" />
                        <span asp-validation-for="StatusName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="เพิ่มข้อมูล" class="btn btn-default" />
                    </div>
                </form>
            </div>
        </div>

        <div>
            <a asp-action="StatusList">กลับสู่รายการ</a>
        </div>
    }
    else
    {
        <h2>คุณไม่มีสิทธิ์การใช้งานหน้านี้</h2>
    }

}
